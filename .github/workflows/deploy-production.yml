name: Deploy to Production

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        default: 'production'
        type: choice
        options:
          - production
          - staging

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  deploy:
    name: Deploy Application
    runs-on: ubuntu-latest
    environment: 
      name: ${{ github.event.inputs.environment }}
      
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull latest Docker image
        run: |
          docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest

      # Example: Deploy to server via SSH
      # Uncomment and configure when you have a deployment target
      # - name: Deploy to server
      #   uses: appleboy/ssh-action@v1.0.0
      #   with:
      #     host: ${{ secrets.SERVER_HOST }}
      #     username: ${{ secrets.SERVER_USER }}
      #     key: ${{ secrets.SSH_PRIVATE_KEY }}
      #     script: |
      #       cd /opt/cair-backend
      #       docker-compose pull
      #       docker-compose up -d --force-recreate
      #       docker image prune -f

      - name: Deployment notification
        run: |
          echo "Deployment to ${{ github.event.inputs.environment }} completed successfully"
          echo "Image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest"

  health-check:
    name: Post-Deployment Health Check
    runs-on: ubuntu-latest
    needs: deploy
    
    steps:
      - name: Wait for application to start
        run: sleep 30

      # Uncomment when you have a production URL
      # - name: Check application health
      #   run: |
      #     response=$(curl -s -o /dev/null -w "%{http_code}" ${{ secrets.APP_URL }}/api/actuator/health)
      #     if [ $response != "200" ]; then
      #       echo "Health check failed with status $response"
      #       exit 1
      #     fi
      #     echo "Health check passed"

      - name: Health check placeholder
        run: |
          echo "Configure health check endpoint in secrets.APP_URL"
          echo "Example: https://api.cair.sdu.edu.kz"

