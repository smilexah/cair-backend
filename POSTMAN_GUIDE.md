# Руководство по использованию Postman коллекции CAIR Backend API

## Импорт коллекции

1. Откройте Postman
2. Нажмите **Import** в верхнем левом углу
3. Перетащите файл `CAIR_API.postman_collection.json` или выберите его через File
4. Коллекция появится в списке коллекций

## Настройка переменных

### Переменные коллекции

Коллекция уже содержит базовые переменные:

- `base_url`: `http://localhost:8080/api` (измените для production)
- `access_token`: автоматически заполняется после логина
- `refresh_token`: автоматически заполняется после логина

### Изменение base_url для production:

1. Нажмите на коллекцию **CAIR Backend API**
2. Перейдите на вкладку **Variables**
3. Измените `base_url` на production URL (например, `https://api.cair.sdu.edu.kz/api`)
4. Нажмите **Save**

## Использование коллекции

### 1. Аутентификация

#### Шаг 1: Логин

1. Откройте запрос **Authentication → Login**
2. В теле запроса укажите credentials:
   ```json
   {
     "username": "admin",
     "password": "admin"
   }
   ```
3. Нажмите **Send**
4. Токены автоматически сохранятся в переменных коллекции

**Ответ (200 OK):**
```json
{
  "accessToken": "eyJhbGciOiJSUzI1NiIsInR5cCI...",
  "refreshToken": "eyJhbGciOiJSUzI1NiIsInR5cCI...",
  "tokenType": "Bearer"
}
```

#### Шаг 2: Использование токена

После успешного логина все защищенные endpoints автоматически будут использовать сохраненный `access_token`.

#### Шаг 3: Обновление токена

Когда токен истекает (через 15 минут):

1. Откройте **Authentication → Refresh Token**
2. Нажмите **Send**
3. Новый access token автоматически сохранится

#### Шаг 4: Выход

1. Откройте **Authentication → Logout**
2. Нажмите **Send**
3. Токен будет деактивирован на сервере

---

### 2. Работа с проектами

#### Создание проекта (требуется ADMIN роль)

1. Откройте **Projects → Create Project**
2. Убедитесь, что вы залогинены
3. Измените данные в теле запроса при необходимости
4. Нажмите **Send**

**Пример тела запроса:**
```json
{
  "slug": "ai-research-project",
  "title": {
    "en": "AI Research Project",
    "ru": "Проект исследования ИИ",
    "kz": "AI зерттеу жобасы"
  },
  "shortDescription": {
    "en": "Advanced AI research and development",
    "ru": "Передовые исследования и разработки ИИ",
    "kz": "Озық AI зерттеу және әзірлеу"
  },
  "fullDescription": {
    "en": "This project focuses on...",
    "ru": "Этот проект фокусируется на...",
    "kz": "Бұл жоба бағытталған..."
  },
  "image": "/projects/ai-project.svg",
  "tags": ["AI", "Machine Learning", "Research"],
  "status": "active",
  "startDate": "2024-01-15",
  "endDate": null,
  "team": ["John Doe", "Jane Smith"],
  "objectives": {
    "en": ["Develop AI models", "Research new algorithms"],
    "ru": ["Разработать модели ИИ", "Исследовать новые алгоритмы"],
    "kz": ["AI модельдерін әзірлеу", "Жаңа алгоритмдерді зерттеу"]
  },
  "results": {
    "en": ["Published 3 papers"],
    "ru": ["Опубликовано 3 статьи"],
    "kz": ["3 мақала жарияланды"]
  }
}
```

**Ответ (201 Created):**
```json
{
  "id": 1,
  "slug": "ai-research-project",
  "title": { ... },
  "shortDescription": { ... },
  "fullDescription": { ... },
  "image": "/projects/ai-project.svg",
  "tags": ["AI", "Machine Learning", "Research"],
  "status": "active",
  "startDate": "2024-01-15",
  "endDate": null,
  "team": ["John Doe", "Jane Smith"],
  "objectives": { ... },
  "results": { ... },
  "createdAt": "2025-11-30T12:00:00",
  "updatedAt": "2025-11-30T12:00:00"
}
```

#### Получение всех проектов

1. Откройте **Projects → Get All Projects**
2. Измените query параметры при необходимости:
   - `page`: номер страницы (начиная с 0)
   - `size`: количество элементов на странице
   - `sortBy`: поле для сортировки (id, slug, status, etc.)
   - `direction`: направление (ASC или DESC)
3. Нажмите **Send**

**Пример URL:**
```
{{base_url}}/projects?page=0&size=10&sortBy=id&direction=ASC
```

**Ответ (200 OK):**
```json
{
  "content": [
    { "id": 1, "slug": "...", ... },
    { "id": 2, "slug": "...", ... }
  ],
  "page": 0,
  "size": 10,
  "totalElements": 25,
  "totalPages": 3,
  "last": false
}
```

#### Получение проекта по ID

1. Откройте **Projects → Get Project by ID**
2. Измените ID в URL (замените `1` на нужный ID)
3. Нажмите **Send**

#### Получение проекта по slug

1. Откройте **Projects → Get Project by Slug**
2. Измените slug в URL
3. Нажмите **Send**

**Пример URL:**
```
{{base_url}}/projects/slug/ai-research-project
```

#### Обновление проекта (требуется ADMIN роль)

1. Откройте **Projects → Update Project**
2. Измените ID в URL
3. Измените данные в теле запроса
4. Нажмите **Send**

#### Удаление проекта (требуется ADMIN роль)

1. Откройте **Projects → Delete Project**
2. Измените ID в URL
3. Нажмите **Send**

**Ответ (204 No Content):**
Пустой ответ, проект успешно удален.

---

### 3. Работа с членами команды

#### Создание члена команды (требуется ADMIN роль)

1. Откройте **Team Members → Create Team Member**
2. Убедитесь, что вы залогинены
3. Измените данные в теле запроса
4. Нажмите **Send**

**Пример тела запроса:**
```json
{
  "name": "John Doe",
  "role": {
    "en": "Senior Researcher",
    "ru": "Старший научный сотрудник",
    "kz": "Аға ғылыми қызметкер"
  },
  "bio": {
    "en": "Expert in AI and Machine Learning",
    "ru": "Эксперт в области ИИ и машинного обучения",
    "kz": "AI және машиналық оқыту саласындағы сарапшы"
  },
  "image": "https://example.com/photos/john-doe.jpg",
  "expertise": ["Machine Learning", "Deep Learning", "NLP"],
  "email": "john.doe@example.com",
  "linkedin": "https://linkedin.com/in/johndoe",
  "github": "https://github.com/johndoe",
  "scholar": "https://scholar.google.com/citations?user=johndoe"
}
```

#### Получение всех членов команды

Аналогично проектам, с поддержкой пагинации.

#### Получение члена команды по ID

Аналогично проектам.

#### Обновление члена команды (требуется ADMIN роль)

Аналогично обновлению проекта.

#### Удаление члена команды (требуется ADMIN роль)

Аналогично удалению проекта.

---

## Автоматизация тестов

Коллекция включает автоматические тесты для каждого запроса:

### Примеры тестов:

1. **Login**:
   - Проверяет статус код 200
   - Проверяет наличие accessToken
   - Автоматически сохраняет токены

2. **Get All Projects**:
   - Проверяет статус код 200
   - Проверяет наличие данных пагинации
   - Проверяет структуру ответа

3. **Create Project**:
   - Проверяет статус код 201
   - Проверяет наличие ID в ответе
   - Сохраняет ID для последующих запросов

### Запуск всех тестов:

1. Нажмите на коллекцию **CAIR Backend API**
2. Нажмите **Run** (иконка с треугольником)
3. Выберите запросы для запуска
4. Нажмите **Run CAIR Backend API**
5. Просмотрите результаты тестов

---

## Environments (Окружения)

Рекомендуется создать отдельные окружения для разных сред:

### Development Environment

1. Нажмите на иконку глаза (👁️) в правом верхнем углу
2. Нажмите **Add** → **Environment**
3. Назовите: `Development`
4. Добавьте переменные:
   ```
   base_url: http://localhost:8080/api
   admin_username: admin
   admin_password: admin
   ```

### Production Environment

1. Создайте новое окружение: `Production`
2. Добавьте переменные:
   ```
   base_url: https://api.cair.sdu.edu.kz/api
   admin_username: <production-username>
   admin_password: <production-password>
   ```

### Переключение между окружениями:

Выберите нужное окружение в dropdown в правом верхнем углу.

---

## Troubleshooting

### Проблема: Токен не сохраняется

**Решение:**
1. Убедитесь, что запрос Login выполнен успешно
2. Проверьте вкладку **Tests** в запросе Login
3. Убедитесь, что скрипт выполняется без ошибок

### Проблема: 401 Unauthorized на защищенных endpoints

**Решение:**
1. Выполните Login запрос заново
2. Проверьте, что `access_token` заполнен в переменных коллекции
3. Проверьте, что токен не истек (TTL: 15 минут)

### Проблема: 403 Forbidden при создании/обновлении/удалении

**Решение:**
- Убедитесь, что вы используете учетную запись с ролью ADMIN
- По умолчанию: username=`admin`, password=`admin`

### Проблема: Connection refused

**Решение:**
1. Убедитесь, что backend запущен
2. Проверьте `base_url` в переменных
3. Проверьте, что порт 8080 доступен

---

## Best Practices

1. **Используйте окружения** для разных сред (dev, staging, production)
2. **Не коммитьте** production credentials в коллекцию
3. **Регулярно обновляйте** токены при длительной работе
4. **Используйте автоматические тесты** для проверки API
5. **Сохраняйте** часто используемые запросы в избранное

---

## Дополнительные функции

### Pre-request Scripts

Вы можете добавить скрипты, выполняющиеся перед запросом:

```javascript
// Автоматический login если токен отсутствует
if (!pm.collectionVariables.get("access_token")) {
    pm.sendRequest({
        url: pm.variables.get("base_url") + "/auth/login",
        method: "POST",
        header: {
            "Content-Type": "application/json"
        },
        body: {
            mode: "raw",
            raw: JSON.stringify({
                username: "admin",
                password: "admin"
            })
        }
    }, function (err, res) {
        pm.collectionVariables.set("access_token", res.json().accessToken);
    });
}
```

### Collection Runner

Для автоматизированного тестирования:

1. Создайте CSV файл с тестовыми данными
2. Используйте Collection Runner
3. Загрузите CSV файл
4. Запустите тесты с разными данными

---

## Swagger UI

Помимо Postman, вы можете использовать Swagger UI:

```
http://localhost:8080/api/swagger-ui.html
```

Swagger предоставляет:
- Интерактивную документацию
- Возможность тестировать API прямо в браузере
- Автоматически сгенерированные схемы запросов/ответов

---

## Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте логи backend: `docker-compose logs backend`
2. Проверьте Swagger документацию
3. Обратитесь к команде разработки

---

Приятного тестирования! 🚀

